export const selectField = (state) => state.field;

export const selectCurrentPlayer = (state) => state.currentPlayer;
export const selectDraw = (state) => state.isDraw;
export const selectGameEnded = (state) => state.isGameEnded;
export const selectStep = (state) => state.step;
